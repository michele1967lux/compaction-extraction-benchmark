{
  "name": "extracted-compaction",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DeepSeek Harness compaction system extracted as an isolated, Cordis-free TypeScript module (session, token meter, and LLM are injected via minimal interfaces).",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
